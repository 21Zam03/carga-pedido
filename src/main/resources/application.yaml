spring:
  application:
    name: carga-pedido
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb
    username: myuser
    password: mypassword
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      jdbc:
        batch_size: 100
        batch_versioned_data: true
      order_inserts: true
      order_updates: true
      hibernate:
        generate_statistics: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  security:
    oauth2:
      resource-server:
        jwt:
          key-value: mi_clave_secreta_super_larga_y_segura_para_jwt_hs512_64_bytes_0123

pedidos:
  batch:
    size: 100
